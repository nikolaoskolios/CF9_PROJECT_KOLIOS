# --- Python (BACKEND) ---
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/

# Live API credential - grants write access to the backend, never commit
api_token.json

# BACKEND/.env
# BACKEND/.env is commented out on purpose: This is a CF9 project / 
# course assignment and the teachers / colleagues should be able to clone and run it with zero setup. In real deployment line 12 will be commented out. 

# --- Node (FRONTEND) - redundant with FRONTEND/.gitignore, kept here too ---
node_modules/
dist/
dist-ssr/
.tanstack/

# --- Editor / OS ---
.vscode/
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db
